# 1) Ignora tudo na raiz
/*

# 2) Un‑ignora qualquer diretório (pra podermos rescatar só o que interessa dentro deles)
!/*/

# 3) Ignora pasta de build
/target/

# 4) Ignora configurações de IDE
/.idea/
/.vscode/

# 5) Un‑ignora arquivos essenciais do projeto
!/.gitignore
!/pom.xml
!/README.md
!/docker-compose.yml
!/Dockerfile
!/mvnw
!/mvnw.cmd

# 6) Maven Wrapper: un‑ignora apenas o wrapper (pra garantir que o mvnw funcione)
/!.mvn/
!/.mvn/wrapper/
!/.mvn/wrapper/maven-wrapper.jar
!/.mvn/wrapper/maven-wrapper.properties

# 7) Un‑ignora todo o seu código‑fonte
!/src/
!/src/**